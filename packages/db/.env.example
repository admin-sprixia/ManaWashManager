# Used only by `prisma generate` locally to satisfy the Prisma CLI — the real runtime
# connection is the D1 binding injected by Cloudflare Workers (see apps/api/wrangler.toml).
DATABASE_URL="file:./dev.db"
